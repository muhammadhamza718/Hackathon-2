[project]
name = "todo-console-application"
version = "0.1.0"
description = "Premium Terminal User Interface for task management with Textual and Rich"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "textual>=0.80.0",
    "rich>=13.7.0",
]
